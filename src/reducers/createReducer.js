import {
  CREATE_LOAN,
  CREATE_LOAN_ERROR,
  CREATE_LOAN_SUCCESS
} from '../actions/agregarActions';

const agregarState = {};

export default function (state = agregarState, action) {
  switch (action.type) {
    default:
      return state;
  }
}
